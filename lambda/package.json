{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-docker": "docker run -v $(pwd):/lambda-project -it lambda-node8 sh -c \"cd /lambda-project && . /root/.nvm/nvm.sh && npm install\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethereumjs-tx": "^1.3.4",
    "web3-js": "^1.0.5-beta.26"
  }
}
